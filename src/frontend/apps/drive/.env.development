NEXT_PUBLIC_S3_DOMAIN_REPLACE=http://localhost:9000
# Leave empty by default so the app can derive the API origin from the current
# browser origin (supports LAN access like http://<server-ip>:3000).
NEXT_PUBLIC_API_ORIGIN=
