# Mirror objects on two buckets

The mirroring has been removed. This feature was pretty hard to maintain and not performant.

Instead we suggest to use synchronization tools like [RClone](https://rclone.org/).
