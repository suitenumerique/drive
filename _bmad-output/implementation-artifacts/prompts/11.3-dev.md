# Prompt (dev) — Story 11.3

Repo: `/root/Apoze/drive` (fork Apoze/drive)

Branch: `story/11.3-ct-s3-evidence-allowlist`

Source of truth (read and follow strictly):
- `_bmad-output/implementation-artifacts/11-3-safe-evidence-allow-listing-for-ct-s3-no-leak-by-construction.md`
- Context: `_bmad-output/project-context.md`

Constraints:
- No-leak by construction: CT-S3 artifacts must never include secrets, raw object
  keys, internal URLs, or credentials.
- Prefer allow-listed evidence fields (status, request_id, hashed identifiers,
  latency buckets, audience codes).
- Deterministic `failure_class` + `next_action_hint`.

Deliverables:
- Implement evidence allow-listing at the producer boundary + regression tests.
- Update traceability:
  - `_bmad-output/implementation-artifacts/runs/<YYYYMMDD-HHMMSS>-11.3/` with
    `report.md`, `commands.log`, `files-changed.txt`, `gates.md`.
  - `_bmad-output/implementation-artifacts/latest.txt`
  - `_bmad-output/implementation-artifacts/sprint-status.yaml`
  - Fill “Dev Agent Record” in the story file.

Verification rule:
- The developer runs lint/tests and records results in the run report.
