# Codex Dev Prompt — Story 12.4 (CI wiring)

Source of truth:
- `_bmad-output/implementation-artifacts/12-4-wire-ct-s3-and-no-leak-scanning-into-ci-with-strict-scope-enforce-dependency-automation-policy.md`

Goal:
- Wire CT-S3 + no-leak scanning into CI via stable gates (Story 12.1).
- Keep scanning scope strict: only `_bmad-output/**` text artifacts.
- Enforce dependency automation policy (Renovate for PRs; Dependabot security only).

Constraints:
- Deterministic failures: `failure_class` + `next_action_hint`, no-leak.
- Avoid scope creep.

Verification:
- `make lint`
- Targeted checks for CI workflow correctness (dry runs if possible).

Traceability:
- Run folder `_bmad-output/implementation-artifacts/runs/<ts>-12.4/` + update `latest.txt` + `sprint-status.yaml` + Dev Agent Record.
