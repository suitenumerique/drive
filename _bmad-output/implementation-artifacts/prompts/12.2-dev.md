# Codex Dev Prompt — Story 12.2 (failure_class + next_action_hint)

Source of truth:
- `_bmad-output/implementation-artifacts/12-2-standardize-failure-class-next-action-hint-across-gates-and-operator-facing-artifacts.md`

Goal:
- Standardize `failure_class` + `next_action_hint` schema across operator artifacts.
- Keep it stable, generic, and no-leak.

Work:
- Align CT-S3 artifacts schema (Epic 11) and other gate artifacts.
- Update docs taxonomy (`docs/failure-class-glossary.md` if still canonical).

Verification:
- `make lint`
- Targeted backend tests proving schema stability/no-leak.

Traceability:
- Run folder `_bmad-output/implementation-artifacts/runs/<ts>-12.2/` + update `latest.txt` + `sprint-status.yaml` + Dev Agent Record.
